#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

    int a=10,b=20;
    scanf("%d%d",&a,&b);
    int r=a*b;
    int g=a+b;
    if(r>1000){
        printf("%d",g);
    }
    else
    {
        printf("%d",r);
    }
    
    return 0;
}
